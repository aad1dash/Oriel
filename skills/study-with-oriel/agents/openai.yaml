interface:
  display_name: "Study with Oriel"
  short_description: "Study videos inside active agent work"
  default_prompt: "Use $study-with-oriel to study this video for what I am doing now."
dependencies:
  tools:
    - type: "mcp"
      value: "oriel"
      description: "Local timestamp-grounded source evidence"
      transport: "stdio"
